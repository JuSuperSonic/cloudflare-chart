{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "configurationSource": {
      "type": "string",
      "enum": ["local", "remote"],
      "description": "Must be either 'local' or 'remote'."
    }
  },
  "required": ["configurationSource"]
}
